version: "3.8"

services:
  web:
    build: .
    image: game-web-app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro # For Nginx
    restart: always
